## Git Instructions

	# to push changes to fork
        git add my_work/
        git commit -m 'sddsfhds'     (by not adding the -a flag, we only commit the directory we added)
		git push origin main

	# Sync upstream content to fork
		git fetch upstream
		git checkout main
		git merge upstream/main


## PDF Instructions

	# Make PDF of completed lecture notebooks and save to "/my_work/" 
